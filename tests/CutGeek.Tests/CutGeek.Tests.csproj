<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>CutGeek.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Skia's native library is not in the box on Linux, and CI runs the checks on ubuntu. -->
    <PackageReference Include="SkiaSharp.NativeAssets.Linux" Version="2.88.9" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/CutGeek.Core/CutGeek.Core.csproj" />
  </ItemGroup>
</Project>
